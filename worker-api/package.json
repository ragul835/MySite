{
  "name": "weraisetech-api-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/*.test.mjs",
    "types": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
